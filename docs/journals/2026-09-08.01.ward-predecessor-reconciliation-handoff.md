# Ward predecessor reconciliation handoff

**Date:** 2026-09-09

## Status

Work is paused immediately after the V32 decisions were applied and the full
rebuild completed through the emitter. The next command is the V33 evidence
fetch; do not repeat the V32 review or refetch its evidence.

The predecessor CREATE batch is **not upload-ready**. Do not submit
`statements/na-wards-create-predecessors.qs` to QuickStatements and do not write
anything to Wikidata. No random-audit round has yet found zero existing items.

The last fully completed pipeline is V32. Its provisional CREATE manifest has
3,094 items: 2,213 xã, 496 phường, and 385 thị trấn, covering 666 former
districts. The emitter is still intentionally gated by the old V2 decision file
and reports `upload_ready=false`.

V32 found two additional existing predecessor items, including one current-map
swap. The rebuild reduced the provisional population from 3,096 to 3,094.

## Exact restart point

The following V32 rebuild stages completed successfully:

| Stage | Result |
|---|---|
| Current ward reconciliation | 3,321 / 3,321 resolved; 0 structural issues |
| Primary predecessor pass | 6,363 / 10,035 auto matched; 3,672 unresolved; 0 issues |
| Broad exact-name pass | 61 / 3,672 auto matched; 3,611 unresolved; 0 issues |
| Article-evidence pass | 179 / 3,543 auto matched; 3,364 unresolved; 0 issues |
| Context pass | 181 / 3,364 auto matched; 3,183 unresolved; 0 issues |
| GeoNames pass | 89 / 3,183 auto matched; 3,094 unresolved; 0 issues |
| CREATE emitter | 3,094 provisional items; 0 unsafe classifications |

Fetch V33 from the rebuilt manifest:

```console
.venv/bin/python -m vn_admin_units.ward_audit_predecessor_gaps \
  --fetch --audit \
  --seed 2026-09-09-predecessor-gap-audit-v33 \
  --evidence-path data/ward-wikidata-predecessor-gap-sample-v33.json \
  --decisions-path data/ward-wikidata-predecessor-gap-sample-v33-decisions.json
```

The fetch requires live Wikidata and Vietnamese Wikipedia access. Review all 50
rows, save the V33 decision file, audit it with `--check --audit`, apply any
findings, rebuild, and continue with V34, V35, and so on until one complete
50-row round has zero existing predecessor items.

## V32 completed review

Saved evidence:
`data/ward-wikidata-predecessor-gap-sample-v32.json`

Saved decisions:
`data/ward-wikidata-predecessor-gap-sample-v32-decisions.json`

The decision audit passes with 50 reviewed rows, two existing items, one
current-map swap, 48 `no-distinct-item-found` outcomes, a 0.04 miss rate, and
`creation_batch_authorized=false`.

The two findings are:

1. Former Hưng Khánh (`w-2131137-base`) → Q32272825. The item retains the exact
   former commune name and class, Yên Bái-area coordinates, and GeoNames
   8526380. Q10771342 remains on the current Hưng Khánh successor.
2. Former Tân An ward (`w-8150119-base`) → Q10733192. The item retains the
   former Ninh Kiều parent, old ward aliases, historical coordinates, and
   GeoNames 8312146. The current Tân An mapping moved from Q10733192 to
   Q137010257, which owns the current `Tân An, Cần Thơ` article and represents
   the 2025 successor formed from Hưng Lợi and An Khánh.

These assignments are recorded in predecessor manual-decision batch
`2026-09-09.27` in
`data/ward-wikidata-predecessor-review-decisions.json`. The Tân An current-map
change is recorded in batch `2026-09-09.91` in
`data/ward-wikidata-review-decisions.json`.

The V31 decision artifact was verified with:

```console
.venv/bin/python -m vn_admin_units.ward_audit_predecessor_gaps \
  --check --audit \
  --seed 2026-09-08-predecessor-gap-audit-v32 \
  --evidence-path data/ward-wikidata-predecessor-gap-sample-v32.json \
  --decisions-path data/ward-wikidata-predecessor-gap-sample-v32-decisions.json
```

It reported 734 fetched candidate items, 15 possible-miss flags, and zero audit
issues.

## Random-audit history

Every round is a reproducible sample of 50 provisional gaps, selected by the
lowest `SHA-256(seed + NUL + local_id)` values. Evidence and decision files are
saved for V2 through V31.

| Round | Population | Existing | Swaps | No item |
|---|---:|---:|---:|---:|
| V1 | — | 14 | — | 36 |
| V2 | — | 1 | 0 | 49 |
| V3 | — | 1 | 0 | 49 |
| V4 | — | 1 | 0 | 49 |
| V5 | — | 1 | 1 | 49 |
| V6 | — | 5 | 4 | 45 |
| V7 | — | 3 | 3 | 47 |
| V8 | — | 1 | 1 | 49 |
| V9 | 3,198 | 2 | 1 | 48 |
| V10 | 3,196 | 4 | 1 | 46 |
| V11 | 3,192 | 4 | 0 | 46 |
| V12 | 3,188 | 2 | 0 | 48 |
| V13 | 3,186 | 4 | 2 | 46 |
| V14 | 3,182 | 5 | 3 | 45 |
| V15 | 3,177 | 2 | 1 | 48 |
| V16 | 3,175 | 5 | 4 | 45 |
| V17 | 3,169 | 4 | 2 | 46 |
| V18 | 3,165 | 7 | 4 | 43 |
| V19 | 3,158 | 6 | 3 | 44 |
| V20 | 3,152 | 8 | 5 | 42 |
| V21 | 3,144 | 11 | 1 | 39 |
| V22 | 3,132 | 2 | 0 | 48 |
| V23 | 3,130 | 5 | 3 | 45 |
| V24 | 3,125 | 3 | 2 | 47 |
| V25 | 3,122 | 4 | 1 | 46 |
| V26 | 3,117 | 6 | 4 | 44 |
| V27 | 3,111 | 3 | 2 | 47 |
| V28 | 3,108 | 3 | 3 | 47 |
| V29 | 3,106 | 1 | 1 | 49 |
| V30 | 3,105 | 5 | 4 | 45 |
| V31 | 3,099 | 3 | 0 | 47 |
| V32 | 3,096 | 2 | 1 | 48 |

Some rounds include a related correction outside the sampled rows, so the next
round's population does not always equal the prior population minus the sampled
findings.

## Full rebuild sequence

Use this complete order whenever a new decision batch changes a current or
predecessor assignment:

```console
.venv/bin/python -m vn_admin_units.ward_reconcile --audit
.venv/bin/python -m vn_admin_units.ward_reconcile_predecessors --verify --rebuild --audit
.venv/bin/python -m vn_admin_units.ward_reconcile_predecessors_broad --fetch --verify --audit
.venv/bin/python -m vn_admin_units.ward_reconcile_predecessors_article --rebuild --audit
.venv/bin/python -m vn_admin_units.ward_reconcile_predecessors_context --rebuild --audit
.venv/bin/python -m vn_admin_units.ward_reconcile_predecessors_geonames --rebuild --audit
.venv/bin/python -m vn_admin_units.ward_emit_predecessor_create --audit
```

The primary and broad stages require live Wikidata access. Preserve stable
`local_id` values and use the repository's `.venv/bin/python`.

## Completion gate

Only after a fully adjudicated random sample has zero existing items:

1. Point `SAMPLE_DECISIONS_PATH` in
   `src/vn_admin_units/ward_emit_predecessor_create.py` at that clean decision
   file.
2. Rebuild the emitter and require `creation_batch_authorized=true` and
   `upload_ready=true`.
3. Update only stale expected counts and decision-path assertions in the tests.
4. Run the affected tests, then the full `.venv/bin/pytest` suite.
5. Run `git diff --check`.

Do not upload the QuickStatements file and do not commit unless explicitly
asked.

## Working-tree warning

The workspace contains extensive generated artifacts, manual decision ledgers,
and V9–V32 evidence/decision files. They are intentional work
from this reconciliation run. Do not reset, clean, checkout, or discard them.

At this checkpoint, the most important new files are:

- `data/ward-wikidata-predecessor-gap-sample-v32.json`;
- `data/ward-wikidata-predecessor-gap-sample-v32-decisions.json`;
- the new `2026-09-09.27` predecessor-decision batch; and
- the new `2026-09-09.91` current-mapping batch.

No final test suite has been run for this checkpoint because the random-audit
loop is still active. The V32 rebuild and decision audit are complete.
