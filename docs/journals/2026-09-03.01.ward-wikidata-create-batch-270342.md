# Current ward creation batch 270342

**Date:** 2026-09-03

## Result

QuickStatements [batch #270342](https://quickstatements.toolforge.org/#/batch/270342)
finished with 151 `DONE` commands and seven `ERROR` commands from the original
158-item current-ward CREATE package.

The public Wikidata contribution log contains exactly 151 item creations tagged
to this batch. Resolving all 158 preflight-pinned Vietnamese Wikipedia titles
after the run produced exactly the same 151 QIDs, with no missing or extra
batch-created item.

One error was not a creation gap by the time the command ran:

- command 18 / manifest sequence 19, `Xã Tam Nông`, now resolves through
  `Tam Nông, Phú Thọ` to existing item Q140391710. Its ward class, Phú Thọ
  parent, 2025 inception, and sitelink identify the intended current unit.

The six other errors created no items and their pages remain unlinked:

| Local ID | Unit | viwiki title |
| --- | --- | --- |
| `w-17818-2025-07-01` | Xã Bích Hào | `Bích Hào` |
| `w-17827-2025-07-01` | Xã Nghi Lộc | `Nghi Lộc (xã)` |
| `w-17833-2025-07-01` | Xã Hải Lộc | `Hải Lộc` |
| `w-17854-2025-07-01` | Xã Văn Kiều | `Văn Kiều` |
| `w-17878-2025-07-01` | Xã Đông Lộc | `Đông Lộc` |
| `w-17920-2025-07-01` | Phường Vinh Hưng | `Vinh Hưng` |

## Durable ingestion

The complete command-to-QID outcome is saved in
`data/ward-wikidata-create-batch-270342.json`. The 151 created QIDs are marked
`qid_status=new`; Q140391710 is marked `existing`. All 152 use
`match_status=manual` and retain the batch number and live sitelink verification
timestamp in `mappings/wards-qid.csv`.

The assignment decisions live in
`data/ward-wikidata-review-decisions.json`, so regenerating the mapping cannot
reopen or overwrite them. Current reconciliation is 3,315/3,321 with zero
structural issues.

## Retry package

The canonical manifest and QuickStatements file were regenerated after
ingestion. They now contain only the six unresolved rows:

- `data/ward-wikidata-create-current.json`
- `statements/na-wards-create-current.qs`

A fresh six-item preflight checked 12 live candidates, rejected all 12, found
all six viwiki pages unlinked, and reports zero duplicates and zero review rows.
Create a new QuickStatements batch from this six-item file. Do not replay batch
#270342 or reset all seven of its errors.
